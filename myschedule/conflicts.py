"""
Conflict detection.

Given events of selected courses, detect overlaps on the same date.
Overlap rule:
    start < other_end AND end > other_start
"""

# TODO (Robert): implement find_conflicts(events).
